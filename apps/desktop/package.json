{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:frontend": "node scripts/build-frontend.mjs",
    "prepare:icons": "node scripts/generate-icons.mjs",
    "dev": "pnpm prepare:icons && pnpm build:frontend && tauri dev",
    "build": "pnpm prepare:icons && pnpm build:frontend && tauri build",
    "tauri": "tauri",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sru/shared": "workspace:*",
    "@tauri-apps/api": "^2.9.1",
    "@tauri-apps/plugin-deep-link": "^2.4.5",
    "@tauri-apps/plugin-notification": "^2.3.3",
    "@tauri-apps/plugin-shell": "^2.3.4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
