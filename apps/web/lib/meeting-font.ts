import { Inter } from "next/font/google";

export const meetingInter = Inter({
  subsets: ["latin", "latin-ext"],
  display: "swap",
  fallback: ["Leelawadee UI", "Noto Sans Thai", "Segoe UI", "sans-serif"],
});
